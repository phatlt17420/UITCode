#include <iostream>

using namespace std;
//xuất node 1 con
XUatNode1con (Tree T)
{
    if()

    if (nút gốc thỏa điều kiện ) // trái rỗng hoặc phải rỗng
        cout<<node gốc;
    XUatNode1con (T->left);
    XUatNode1con (T->right);
}
int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
